# SSS（Short Swing Score）研究プロジェクト

日本株の短期売買（初心者・実際の売買前の研究段階）用の独自スコアを作り、過去データで検証するプロジェクト。
回答は日本語で、初心者にも分かる言葉で。事実と推測を分け、推測には「推測」と明記する。売買の推奨はしない。

## 本人の売買ルール（シミュレーションの前提）
- 資金100万円、使うのは最大60万円。1銘柄20〜30万円、同時保有は最大2銘柄、現物のみ
- 1取引の最大損失5,000円、1日の最大損失1万円、月-5万円でその月は取引停止
- 決算をまたがない。金曜は持ち越さない。証券会社は楽天証券（iSPEED）

## ファイル
- `sss_collect.py` : yfinance で約100銘柄（11セクター）＋TOPIX連動ETF(1306)の日足5年分を取得し `sss_data.json` を出力
- `tool/sss-lab.html` : 重み調整・学習・検証ツール（公開中のArtifact「SSSスコア研究室」のソース。Artifactの外枠なしの本文）
  - 6要素: トレンド / 対TOPIX相対強度(20日) / 出来高倍率(20日平均比) / 5日モメンタム(上がりすぎは減点) / 安定性(ATR14÷株価) / 押し目(25日線上向き×5日騰落−1〜−6%で満点、0〜−1%で0.5、−6%超の下げで0.3)
  - 重みはマイナス可。点数=(Σw·f − Σ負のw)/(Σ正のw − Σ負のw)×100
  - 減点: 5日+20%以上、20日平均売買代金10億円未満、始値+4%以上の窓開け、前日+10%以上
  - 売買シミュ: 翌朝始値で買い、利確+5%/損切り-2.5%/最長5日（同日両方は損切り優先）、コスト0.1%
  - 学習: リッジ回帰（目的変数=上記シミュの損益、±15%でクリップ）。係数は符号つきで絶対値合計100に正規化。セクター別は件数に応じて全体の係数へ縮小（K=件数換算）
  - ウォークフォワード検証（年ごとに過去のみで再学習）、直近データのリプレイ運用（開始日以降は未学習）

## 次にやること
1. `pip install yfinance` → `python sss_collect.py` で実データ取得（ネットワークはCustom環境で Yahoo Finance を許可済みの想定）
2. 手動(25/25/20/15/15)・均等・学習(全体)・学習(セクター別)をウォークフォワードで比較し、どの要素が効いているか分析
3. 決算日データ（J-Quants）の追加、毎朝のスコア自動計算

## 検証結果メモ（2026-09-25、96銘柄・2021-09〜2026-09、ウォークフォワード2023〜2026、75点・1日2銘柄）
- 基準（全銘柄を毎日買った場合）: +0.165%/取引
- 専門家案(25/25/20/15/15): +0.07%（基準に負け）。上昇トレンド・5日上昇が強い株ほど成績が悪い（短期の反動）
- 符号つき学習・全体: +0.15〜0.27%、セクター別: +0.13〜0.15%（どちらも基準との差は誤差の範囲、年ごとに不安定）
- 押し目型プリセット(0/30/0/−15/15/40): +0.25%、4年中3年で基準超え。ただし全期間データを見てから作った設定なので過大評価の可能性あり
- 結論: 過去データでは確かな優位性なし。押し目型を「事前に決めた仮説」として、2026-09-26以降の紙上売買で検証する

## 紙上売買（2026-09-28 開始・フォワードテスト）
- `sss_paper.py` : 平日の引け後に実行。データ取得（直近1年）→ 前日に決めた注文を当日始値で約定 → 損切り/利確/期限/週末で決済 → 当日終値で点数を計算し翌日の注文予定を記録
- `paper_config.json` : 配点（押し目型 0/30/0/−15/15/40）とルールを固定。**検証中は変更しない**
- `records/` : paper_state.json（保有・注文予定）、paper_trades.csv（決済済み）、signals.csv（毎日の上位10）、daily/日付.md（日次レポート）
- `python sss_paper.py --report` : 検証開始以降の成績を「全銘柄を毎日買った場合」と比較（100件を超えるまで判断しない）
- 15:45 JST 前は実行を拒否。当日のデータがなければ休場とみなして何もしない。同じ日の二重実行も防止
- 実行後は records/ を main ブランチへ直接コミット＆プッシュする（次回の実行が状態を引き継ぐため）
