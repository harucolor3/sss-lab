# SSS（Short Swing Score）研究プロジェクト — 現行仕様 v1.4（2026-09-25 固定）

日本株の短期売買のための独自スコアを作り、過去データと将来データ（紙上売買）で検証する研究プロジェクト。実際の発注はしない。
回答は日本語で、初心者にも分かる言葉で。事実と推測を分け、推測には「推測」と明記する。売買の推奨はしない。
詳細は仕様書 PDF（SSSスコア研究室_仕様書_v1.4）を参照。このファイルは常に現行仕様だけを書く（古い記述を残さない）。

## 絶対に変えないもの（2026-09-28〜のフォワードテスト中）
- 配点（押し目型）: トレンド 0 / 対市場 +30 / 出来高 0 / 5日騰落 −15 / 安定性 +15 / 押し目 +40
- 減点: 5日+20%以上 15、20日平均売買代金10億円未満 20、始値+4%以上の窓開け 10、前日+10%以上 15
- 基準点 75点、利確 +5%、損切り −2.5%、最長保有 5営業日、コスト 0.1%
- 変更が必要になったら「次世代の検証」として start_date と records/ を分けて別に行う。現行の検証を上書きしない

## 本人の売買ルール（紙上売買の前提）
- 総資金100万円、うち運用資金60万円（capital）。現物のみ。同時保有は最大2銘柄
- 1取引の許容損失 5,000円（通常の値動きを前提とした目安。窓開けで超えることがあり絶対上限ではない）
- 株数 = min(許容損失 ÷ (買値×(2.5%+0.1%)), 1銘柄上限30万円 ÷ 買値, 資金残り ÷ 買値) を100株単位で切り捨て
  → 100株買える株価は約1,923円以下。96銘柄のうち実際に買えるのは約20銘柄（2026-09-25時点）
- 損失の停止基準: 1日1万円に達したら翌日は新規購入なし／1か月5万円に達したらその月は新規購入なし（損失の絶対上限ではない）
- 決算をまたがない。週の最終営業日（金曜、祝日なら前日）は持ち越さない。証券会社は楽天証券（iSPEED）

## ファイル
- `sss_collect.py` : yfinance で96銘柄（独自11セクター）＋TOPIX連動ETF 1306（始値・終値）の日足を取得し `sss_data.json` を出力。分割履歴 `splits` も保存。`--earnings` で過去の決算日も取得
- `sss_paper.py` : 紙上売買の日次処理と成績レポート。`--report` `--check-earnings` `--no-fetch` `--force`
- `paper_config.json` : 上記の固定設定と評価基準（evaluation）
- `tool/sss-lab.html` : 研究用ツール「SSSスコア研究室」（claude.ai の Artifact と同じ内容）。バックテスト・学習・ウォークフォワード・リプレイ
- `records/` : 紙上売買の記録（ルーティンが作成。手で編集しない）
- `sss_universe_test.py` : 研究用。既定96銘柄と拡大版（`sss_collect.py --universe extended`：売買が活発なプライム株を追加、直近60日の平均売買代金10億円以上）で、直近60営業日の損益（均等・専門家案・押し目型・直近60日より前で最適化した配点・75点以上ランダム・TOPIX）と、追加銘柄だけでの押し目型の研究評価を比較。結果は `universe_report.md`。紙上売買の設定・記録には触れない
- `sss_backtest_pre.py` : 押し目型の作成に使っていない期間（既定 2011-01-01〜2021-09-24）で、固定設定のまま研究評価・実運用評価を行う。結果は `pre_period_report.md`。records/ には書き込まない。決算回避なし。株数は分割履歴から当時の実際の株価で計算（`sss_collect.py --start --end --full-splits`）
- 依存: `pip install yfinance pandas numpy jpholiday`

## 紙上売買の日次処理（平日16:45 ルーティン、Claude Code クラウド環境 stock-data）
1. 15:45 JST 前は実行しない。当日のデータがなければ休場として何もしない。処理済みの日は二重に処理しない
2. 次回決算日を取得（yfinance。取得失敗時はキャッシュ済みの未来日付を保持）。保有中の株式分割を検出したら買値・株数を補正
3. 前夜の注文予定（75点以上の全候補を順位順）を当日始値で約定。買えない銘柄（許容損失・上限金額・資金不足）は飛ばして次順位で補充
4. 保有銘柄を 損切り → 利確 → 保有期限 → 週末手仕舞い → 決算前の手仕舞い の順で判定。同日に利確・損切り両方に届いたら損切り優先
   - 処理順序は「新規約定 → 決済判定」。朝の窓開けで枠が空いてもその日は新規に買わない（前夜に注文を出す運用の再現）
5. 損失の停止基準を判定
6. 当日終値で全銘柄の点数を計算。75点以上のうち保有予定期間（翌営業日〜+4営業日）に決算がない銘柄を翌日の注文予定に
   - 決算日が不明な銘柄は、直近60日以内に決算済みなら可、それ以外は買わない
7. 記録して records/ を main ブランチへ直接コミット＆プッシュ。金曜は `--report` も実行
- 営業日は東証カレンダー（土日・祝日〔jpholiday〕・12/31・1/1〜1/3）で判定

## 記録（当日の状態のまま保存し、後から再計算で置き換えない）
- `records/signals.csv` : 75点以上の全シグナル（1日の数制限なし）。要素値・減点・終値・100株買えるか・決算日・注文予定に入ったか
- `records/scores/YYYY-MM-DD.csv` : 全銘柄の点数と要素（研究評価の母集団）
- `records/paper_trades.csv` : 決済済みの紙上売買 ／ `records/paper_state.json` : 保有・注文予定・停止状態
- `records/earnings_log.json` : その日に分かっていた次回決算日 ／ `records/earnings_cache.json` : 次回・直近の決算日
- `records/daily/YYYY-MM-DD.md` : 日次レポート（上位10と「買える見込み／飛ばす」）

## 評価（--report）
- 研究評価（全96銘柄の銘柄選択力）: 当日記録の全シグナル・上位2 の期待値 − 全銘柄平均。週単位ブートストラップ5,000回の95%信頼区間
- 実運用評価・必須（紙上売買と同じ売買エンジン）:
  ① 75点以上の候補からランダムに選ぶ運用（200回）の中で、SSS の最終損益が上位5%以内（ランキングに意味があるか）
  ② 点数を使わず全銘柄からランダムに選ぶ運用の1取引期待値との差の95%信頼区間の下限がプラス（銘柄選択に意味があるか）
  ③ 月間損益（確定ベース）の最低が −5万円より上（PASS/FAIL）
  ④ TOPIXの買い持ち（60万円を最初に売買できる日の始値で買って持ち続ける）より損益が大きい（PASS/FAIL。2026-09-26 に正式な比較対象に追加。
     2023-01〜2026-09 の試算では TOPIX買い持ち +70.7万円に対し、SSS押し目型は +28.9万円で大きく負けていた）
- 配当は0円として計算（権利落ちの下落のみ損失に含む保守的評価）
- 段階: 暫定評価 = 3か月以上かつ全シグナル300件以上／本格評価 = 6か月以上かつ全シグナル500件以上かつ決済済み100回以上（現ペースで約9か月）
- 実資金へ進む条件: 本格評価の段階で、研究評価の下限プラス、かつ実運用評価①②③④すべて合格。100回程度で結論を出さない

## 並行トラックと対象銘柄（2026-09-26 決定・2026-09-28 開始）
- 対象銘柄: 既定96銘柄＋追加候補（売買が活発なプライム株）のうち、固定日時点の直近60営業日の平均売買代金10億円以上。
  `sss_setup_tracks.py` を開始前に1回だけ実行して `universe.json` に固定。毎日の取得はこの一覧だけ（日々入れ替えない）
- トラックA: 押し目型（配点は不変）… `paper_config.json` → 記録 `records/`
- トラックB: 全体で1つの配点（マイナス可）。拡大後の全銘柄・全期間（最後の保有期間分を除く）でリッジ回帰により学習し、
  `sss_setup_tracks.py` が `paper_config_b.json` に固定 → 記録 `records_b/`（旧案の業種別トラックは開始前に取りやめ）
- 日次: `python sss_paper.py` の後に `python sss_paper.py --config paper_config_b.json --reuse-data`
- レポート: `python sss_paper.py --report` と `python sss_paper.py --config paper_config_b.json --report`
- 検証中は universe.json・両トラックの設定を変更しない。`sss_setup_tracks.py` を再実行しない

## 直近1年のウォークフォワード検証（2026-09-26、60営業日×4期間、各期間の直前までで学習→その期間で運用）
- 合計損益: 押し目型（後出し）+164,250／TOPIX買い持ち +157,289／全体学習・符号つき +117,739／業種別・符号つき +78,414／
  格子最適化 +45,546／全銘柄ランダム中央値 +22,170／業種別・プラスのみ（取りやめた旧トラックB案）+20,183／専門家案 +13,940／均等 +8,225／全体学習・プラスのみ −24,847
- 後出しでない方式で TOPIX買い持ちに勝ったものはなし。どの方式も TOPIX に勝ったのは4期間中1期間

## 過去データでの結果（参考。押し目型は全期間を見た後に作った後出しの設定）
- 96銘柄・2021-09〜2026-09、ウォークフォワード2023〜2026: 押し目型 +0.258%/取引 vs 全銘柄 +0.165%。差の95%CI −0.07〜+0.26%（未確認）
- 同じ日・同じ件数のランダム選択に対して上位23%。専門家案（上昇株ほど高得点）は +0.07% で基準未満（大型株の短期の反動）
- 直近60営業日（押し目型の作成時に見た期間。動作確認のみ）: 32取引 +68,846円。75点以上からのランダムに対しては上位15%程度

## 次世代の検討項目（現行の検証では変更しない）
- 5日騰落と押し目の二重評価（なし/片方/両方）、配当込み損益 or 権利落ちをまたぐ取引の除外、J-Quants（東証カレンダー・決算予定・構成銘柄）への移行、低位株を加えた対象銘柄の拡大

## 次世代の研究: 決算発表後のドリフト（2026-09-26 追加。紙上売買とは別）
- `sss_pead.py` : J-Quants API V2 `/v2/fins/summary`（ヘッダー x-api-key、環境変数 JQUANTS_API_KEY）で211銘柄の決算を取得し jq_fins.json に保存。
  予想修正率・営業利益の前年同期比・来期予想の増益率で分類し、発表後に最初に買える日の始値から1/5/20/60日の対TOPIX超過リターンを集計 → pead_report.md
- 分類の基準（上方/下方修正±5%、前年比±20%、来期予想±10%）は結果を見て変えない
- 無料プランは約2年分・12週間遅れ・5回/分（--sleep 12）。ライト以上で5年分（料金は公式で確認）
